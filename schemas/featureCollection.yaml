type: object
required:
  - type
  - features
properties:
  type:
    type: string
    enum:
      - FeatureCollection
  features:
    type: array
    items:
      $ref: featureCityJSON.yaml
  links:
    type: array
    items:
      $ref: link.yaml
  timeStamp:
    $ref: timeStamp.yaml
  numberMatched:
    $ref: numberMatched.yaml
  numberReturned:
    $ref: numberReturned.yaml